/// Animation duration tokens for Brilliant Movee.
abstract final class AppDurations {
  static const Duration instant = Duration(milliseconds: 0);
  static const Duration fast = Duration(milliseconds: 150);
  static const Duration normal = Duration(milliseconds: 300);
  static const Duration slow = Duration(milliseconds: 500);
  static const Duration slower = Duration(milliseconds: 800);
  static const Duration splash = Duration(milliseconds: 2500);
  static const Duration pieceMove = Duration(milliseconds: 300);
  static const Duration badgeShow = Duration(milliseconds: 1500);
  static const Duration brilliantGlow = Duration(milliseconds: 2000);
  static const Duration evalBar = Duration(milliseconds: 400);
  static const Duration celebrate = Duration(milliseconds: 1200);
  static const Duration shimmer = Duration(milliseconds: 1200);
  static const Duration typewriter = Duration(milliseconds: 60);
}
